#include<stdio.h>
int main()
{
    int i,n,arr[n];
    scanf("%d\n",&n);
    for(i=0;i<n;i++){
         scanf("%d",&arr[n]);
    }
    if (ascending) {
        return Sorted;
    } else if (descending) {
        return -1;
    } else {
        return 0; 
    }
}